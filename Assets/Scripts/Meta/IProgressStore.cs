/// <summary>
/// Хранилище прогресса. Интерфейс введён сразу, хотя реализация пока одна:
/// по dev-plan.md в Фазе 4 прогресс переезжает на собственный Go-бэкенд,
/// и подмена должна стоить одну строку в бутстрапе, а не правку игровой логики.
/// </summary>
public interface IProgressStore
{
    /// <summary>Загрузить прогресс. Никогда не возвращает null — при отсутствии или повреждении данных отдаёт чистый прогресс.</summary>
    PlayerProgress Load();

    void Save(PlayerProgress progress);
}
